package com.realdolmen.shopr.service;


import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

public class SupplierServiceTest {

    private SupplierService service;

    @Before
    public void init() {
        service = new SupplierService();
    }

    @Test
    public void testShouldThrowException() {
//        Assert.assertTrue(service.shouldThrowException(0.00));
//        Assert.assertTrue(service.shouldThrowException(2.11));
//        Assert.assertTrue(service.shouldThrowException(2.12));
//        Assert.assertTrue(service.shouldThrowException(2.81));
//        Assert.assertTrue(service.shouldThrowException(2.82));
//        Assert.assertFalse(service.shouldThrowException(3.11));
//        Assert.assertFalse(service.shouldThrowException(3.12));
//        Assert.assertFalse(service.shouldThrowException(3.81));
//        Assert.assertFalse(service.shouldThrowException(3.82));
    }

}
